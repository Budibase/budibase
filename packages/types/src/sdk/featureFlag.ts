export enum FeatureFlag {
  AI_AGENT_ACTIVITY = "AI_AGENT_ACTIVITY",
  AI_TESTS = "AI_TESTS",
  FRONT_COMPANION = "FRONT_COMPANION",
  PROJECTS = "PROJECTS",
  // Dev
  FEATURE_FLAG_OVERRIDES = "FEATURE_FLAG_OVERRIDES",
  DEV_USE_CLIENT_FROM_STORAGE = "DEV_USE_CLIENT_FROM_STORAGE",
}

export type FeatureFlags = Record<FeatureFlag, boolean>
