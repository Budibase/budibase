import { it, expect, describe, beforeEach, vi } from "vitest"
import { getColumns } from "./getColumns"

describe("getColumns", () => {
  describe("converting old column format", () => {
  })

  describe("default columns", () => {
  })

  describe("missing columns", () => {
  })

  describe("invalid columns", () => {
  })
})
