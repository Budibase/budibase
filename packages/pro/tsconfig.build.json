{
  "extends": "../../tsconfig.build.json",
  "compilerOptions": {
    "outDir": "dist",
    "paths": {}, // We don't want to bundle anything, so we avoid duplicated packages on the final project bundled
    "sourceMap": false
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "./**/*.spec.ts", "./**/*.spec.js"]
}
