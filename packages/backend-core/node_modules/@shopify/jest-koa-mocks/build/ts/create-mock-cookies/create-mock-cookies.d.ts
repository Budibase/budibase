import { Context } from 'koa';
export declare type Cookies = Context['cookies'];
export interface Dictionary<T> {
    [key: string]: T;
}
export interface MockCookies extends Cookies {
    requestStore: Map<string, string>;
    responseStore: Map<string, string>;
}
export default function createMockCookies(cookies?: {}, secure?: boolean): MockCookies;
//# sourceMappingURL=create-mock-cookies.d.ts.map