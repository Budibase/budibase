server {
  listen 80 default_server;
  location / {
    default_type application/json;
    return 200 '{"x_real_ip":"$http_x_real_ip","x_forwarded_for":"$http_x_forwarded_for","remote_addr":"$remote_addr"}';
  }
}
